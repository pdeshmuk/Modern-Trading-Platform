accelerator:
  displayName: Modern Trading Platform
  description: A sample trading platfrom using REST services built with Tanzu supply-chain
  iconUrl: https://github.com/pdeshmuk/Modern-Trading-Platform/blob/main/icon-trading.jpeg
  tags:
  - java
  - spring
  - web
  - tanzu

  options:
  - name: repositoryPrefix
    inputType: text
    label: Prefix for the container image repository
    defaultValue: dev.local
    required: true

engine:
  merge:
  - include: [ "**/*" ]
    exclude: [ "config/*.yaml", "Tiltfile", "README.md", "catalog/*.yaml" ]
  - include: [ "config/*.yaml", "Tiltfile" ]
    chain:
    - type: ReplaceText
      substitutions:
      - text: modern-trading-platform
        with: "#artifactId"
    - type: ReplaceText
      substitutions:
      - text: your-registry.io/project
        with: "#repositoryPrefix"
  - include: [ "README.md" ]
    chain:
    - type: ReplaceText
      substitutions:
      - text: modern-trading-platform
        with: "#artifactId"
  - include: [ "catalog/*.yaml" ]
    chain:
    - type: ReplaceText
      substitutions:
      - text: modern-trading-platform
        with: "#artifactId"
    - type: RewritePath
      rewriteTo: "#filename"